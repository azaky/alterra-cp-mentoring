#include <bits/stdc++.h>
using namespace std;

int solve(int n) {
  // Letakkan implementasi Anda di sini.
  return -1;
}

int main() {
  int n;
  scanf("%d", &n);
  printf("%d\n", solve(n));

  return 0;
}