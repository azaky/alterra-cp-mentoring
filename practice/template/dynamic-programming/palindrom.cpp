#include <bits/stdc++.h>
using namespace std;

int solve(const string& s) {
  // Letakkan implementasi Anda di sini.
  return -1;
}

int main() {
  int t;
  scanf("%d", &t);
  for (int i = 0; i < t; ++i) {
    static char buffer[100];
    scanf("%s", buffer);
    printf("%d\n", solve(buffer));
  }

  return 0;
}
